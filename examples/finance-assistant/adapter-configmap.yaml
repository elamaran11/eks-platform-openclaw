apiVersion: v1
kind: ConfigMap
metadata:
  name: finance-adapter-src
  namespace: finance-assistant
data:
  server.js: |
    // Adapter sidecar — injected via ConfigMap; lives in the same Kata VM
    // as the openclaw gateway. Calls the warm local gateway via `openclaw
    // agent` per turn, targeting the user's per-user agent.

    // Log redactor: belt-and-braces scrub of stdout/stderr for secret-
    // shaped strings. Primary defense is still tmpfs-mounted secrets +
    // env-strip below.
    (function installLogRedact() {
      const PATTERNS = [
        /\bsk-[A-Za-z0-9_-]{16,64}\b/g,
        /\b(AKIA|ASIA)[A-Z0-9]{16}\b/g,
        /\beyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\b/g,
        /\b[a-f0-9]{48,}\b/g,
        /\b[A-Za-z0-9/+=]{40}\b/g,
      ];
      function scrub(c) {
        if (c == null) return c;
        let s = typeof c === "string" ? c : c.toString("utf8");
        for (const re of PATTERNS) s = s.replace(re, "[REDACTED]");
        return typeof c === "string" ? s : Buffer.from(s, "utf8");
      }
      for (const stream of [process.stdout, process.stderr]) {
        const orig = stream.write.bind(stream);
        stream.write = (c, e, cb) => orig(scrub(c), e, cb);
      }
    })();

    const http = require("http");
    const fs = require("fs");
    const { spawn } = require("child_process");

    const PORT = parseInt(process.env.PORT || "18790", 10);
    const OPENCLAW_CMD = process.env.OPENCLAW_CMD || "openclaw";
    // The gateway reads its config/agents from /home/node/.openclaw (a
    // shared emptyDir mounted in both containers). `agents add` and
    // `agent --agent` MUST run with this HOME so the add lands in the
    // config the running gateway hot-reloads — otherwise --agent silently
    // falls back to the shared `main` agent and per-user routing breaks.
    const OPENCLAW_HOME = process.env.OPENCLAW_HOME || "/home/node";
    const rawArgs = process.env.OPENCLAW_ARGS || "";
    const OPENCLAW_ARGS = rawArgs.trim() ? rawArgs.trim().split(/\s+/) : [];
    // 15 min. Cold-start on a fresh per-user sandbox installs openclaw's
    // bundled plugins (amazon-bedrock SDK, acpx, etc.) before the first
    // response; that can take 60-120s on top of normal LLM time. Short
    // timeouts truncate the response and make the UI look frozen.
    const DEFAULT_TIMEOUT_MS = parseInt(process.env.ADAPTER_TIMEOUT_MS || "900000", 10);
    // Shared EFS root; each user gets /workspace/users/<suffix>.
    const WORKSPACE_ROOT = process.env.WORKSPACE_DIR || "/workspace";
    // Suffix is a 10-hex-char hash from the router. Validate defensively so
    // it can never escape the users/ dir or inject shell/flag args.
    const SUFFIX_RE = /^[a-z0-9]{1,32}$/;

    // Secrets are tmpfs-mounted as files, not env vars. Read at startup
    // and drop from env so any downstream `env` dump leaks nothing.
    function readSecret(p) { try { return fs.readFileSync(p, "utf8").trim(); } catch { return ""; } }
    const LITELLM_API_KEY = readSecret("/var/run/openclaw/litellm-api-key") || process.env.LITELLM_API_KEY || "";
    const GATEWAY_AUTH_TOKEN = readSecret("/var/run/openclaw/gateway-auth-token") || process.env.GATEWAY_AUTH_TOKEN || "";
    delete process.env.LITELLM_API_KEY;
    delete process.env.GATEWAY_AUTH_TOKEN;

    function agentId(suffix) { return `pod-agent-${suffix}`; }
    function userWorkspace(suffix) { return `${WORKSPACE_ROOT}/users/${suffix}`; }

    // Per-user agent provisioning is idempotent + deduped. `provisioned`
    // caches suffixes added this gateway lifetime; `inflight` shares one
    // promise so two concurrent first-hits don't double-add.
    const provisioned = new Set();
    const inflight = new Map();

    // Provision (or heal) the user's openclaw agent. Pre-creating the
    // workspace dir makes `agents add`'s own mkdir a no-op — sidestepping
    // its non-atomic config-then-mkdir behavior and any EFS uid surprise.
    function provisionAgent(suffix) {
      return new Promise((resolve, reject) => {
        const ws = userWorkspace(suffix);
        try { fs.mkdirSync(ws, { recursive: true }); }
        catch (e) { return reject(new Error(`workspace mkdir failed: ${e.message}`)); }
        const args = [...OPENCLAW_ARGS, "agents", "add", agentId(suffix),
          "--workspace", ws, "--non-interactive"];
        const child = spawn(OPENCLAW_CMD, args, {
          env: { PATH: process.env.PATH, HOME: OPENCLAW_HOME, NODE_ENV: process.env.NODE_ENV || "production" },
          stdio: ["ignore", "pipe", "pipe"],
        });
        let out = "";
        child.stdout.on("data", (d) => { out += d.toString(); });
        child.stderr.on("data", (d) => { out += d.toString(); });
        child.on("close", (code) => {
          // Idempotent: re-add of an existing agent is success.
          if (code === 0 || /already exists/i.test(out)) return resolve();
          console.error(`[adapter] agents add exit ${code}: ${out.slice(0, 300)}`);
          reject(new Error(`agents add failed (exit ${code})`));
        });
      });
    }

    function ensureAgent(suffix) {
      if (provisioned.has(suffix)) return Promise.resolve();
      const existing = inflight.get(suffix);
      if (existing) return existing;
      const p = provisionAgent(suffix)
        .then(() => { provisioned.add(suffix); })
        .finally(() => { inflight.delete(suffix); });
      inflight.set(suffix, p);
      return p;
    }

    function runOpenclaw(sessionId, message, suffix) {
      return new Promise((resolve, reject) => {
        // Use warm gateway (omit --local) so we skip the agent cold start.
        // --agent targets the user's dedicated agent (overrides routing
        // bindings) so the turn uses that user's workspace subdir.
        const args = [...OPENCLAW_ARGS, "agent"];
        if (suffix) args.push("--agent", agentId(suffix));
        args.push("--session-id", sessionId, "--thinking", "off", "--json", "-m", message);
        const child = spawn(OPENCLAW_CMD, args, {
          env: { PATH: process.env.PATH, HOME: OPENCLAW_HOME, NODE_ENV: process.env.NODE_ENV || "production" },
          stdio: ["ignore", "pipe", "pipe"],
        });

        let stdout = "";
        let stderr = "";
        const timer = setTimeout(() => {
          child.kill("SIGTERM");
          reject(new Error(`openclaw timeout after ${DEFAULT_TIMEOUT_MS}ms`));
        }, DEFAULT_TIMEOUT_MS);

        child.stdout.on("data", (d) => { stdout += d.toString(); });
        child.stderr.on("data", (d) => { stderr += d.toString(); });
        child.on("close", (code) => {
          clearTimeout(timer);
          // Warm path: {result:{payloads:[{text}]},status:"ok"}
          const candidates = [stdout, stderr];
          for (const src of candidates) {
            const jsonStart = src.indexOf("{\n");
            if (jsonStart < 0) continue;
            try {
              const parsed = JSON.parse(src.slice(jsonStart));
              const payloads = parsed?.result?.payloads ?? parsed?.payloads;
              if (!payloads) continue;
              const text = payloads?.[0]?.text ?? "";
              const stopReason = parsed?.result?.meta?.stopReason
                ?? parsed?.meta?.stopReason
                ?? parsed?.status
                ?? "unknown";
              return resolve({ text, stopReason });
            } catch { continue; }
          }
          console.error(`[adapter] openclaw exit ${code}`);
          console.error(`[adapter] stdout(${stdout.length}):`, stdout.slice(0, 500));
          console.error(`[adapter] stderr(${stderr.length}):`, stderr.slice(0, 500));
          reject(new Error(`openclaw exit ${code}; stdout=${stdout.length}B stderr=${stderr.length}B`));
        });
      });
    }

    function sseWrite(res, data) { res.write(`data: ${JSON.stringify(data)}\n\n`); }

    async function streamReplyAsSse(res, text) {
      // openclaw currently returns the whole reply synchronously, so there
      // is no token stream to forward. Emit once — the previous 8-char
      // chunk + 10ms sleep loop added ~2s of pointless latency *after*
      // the model was already done. True per-token streaming is a
      // follow-up (needs the openclaw WS client).
      sseWrite(res, { delta: text });
      sseWrite(res, { done: true });
      res.write("data: [DONE]\n\n");
      res.end();
    }

    const server = http.createServer(async (req, res) => {
      if (req.method === "GET" && req.url === "/healthz") {
        res.writeHead(200); return res.end("ok\n");
      }
      if (req.method === "POST" && req.url === "/chat") {
        let body = "";
        req.on("data", (c) => { body += c; });
        req.on("end", async () => {
          let heartbeat;
          try {
            const { message, sessionId = "web", suffix } = JSON.parse(body || "{}");
            if (!message) {
              res.writeHead(400, { "Content-Type": "application/json" });
              return res.end(JSON.stringify({ error: "message required" }));
            }
            // A valid suffix routes to the user's dedicated agent. Absent or
            // malformed (legacy/smoke path) falls back to the default agent.
            const useSuffix = typeof suffix === "string" && SUFFIX_RE.test(suffix) ? suffix : null;
            res.writeHead(200, {
              "Content-Type": "text/event-stream",
              "Cache-Control": "no-cache, no-transform",
              "Connection": "keep-alive",
              "X-Accel-Buffering": "no",
            });
            sseWrite(res, { status: "thinking" });
            // Heartbeats every 10s prevent ALB 60s idle timeout from killing
            // the stream while openclaw is still computing.
            heartbeat = setInterval(() => {
              try { res.write(": ping\n\n"); } catch {}
            }, 10000);

            // Provision the per-user agent on first interaction (idempotent).
            if (useSuffix) await ensureAgent(useSuffix);

            const { text, stopReason } = await runOpenclaw(sessionId, message, useSuffix);
            clearInterval(heartbeat);
            if (stopReason === "error" || !text) {
              sseWrite(res, { error: text || "agent error", stopReason });
              res.write("data: [DONE]\n\n");
              return res.end();
            }
            await streamReplyAsSse(res, text);
          } catch (e) {
            clearInterval(heartbeat);
            console.error("[adapter]", e.message);
            try { sseWrite(res, { error: e.message }); res.write("data: [DONE]\n\n"); res.end(); } catch {}
          }
        });
        return;
      }
      res.writeHead(404); res.end();
    });

    server.listen(PORT, "0.0.0.0", () => console.log(`[adapter] listening on :${PORT}`));
